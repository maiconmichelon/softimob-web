<nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to 'Softimob', "/", class: "navbar-brand"%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_user %>
        <% if current_empresa %>
          <%= render 'layouts/navbar_left_with_login_and_empresa' %>
        <% else %>
          <%= render 'layouts/navbar_left_with_login_without_empresa' %>
        <% end %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
            <%= render 'layouts/navbar_right_with_login' %>
        <% else %>
          <li><%= link_to "Login", "/auth/google_oauth2", id: "sign_in" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
