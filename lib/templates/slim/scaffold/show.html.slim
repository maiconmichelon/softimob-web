= show_for(@<%= singular_name %>) do |s|
<%- attributes.each do |attribute| -%>
  = s.<%= attribute.reference? ? :association : :attribute %> :<%= attribute.name %>
<% end -%>

= link_to 'Editar', edit_<%= singular_name %>_path(@<%= singular_name %>)
|
= link_to 'Voltar', <%= plural_name %>_path
